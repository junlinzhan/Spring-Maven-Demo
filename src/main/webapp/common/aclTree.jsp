<%@ page contentType="text/html;charset=UTF-8" language="java" %><script id="aclTreeTemplate" type="x-tmpl-mustache"><ol class="dd-list ">    {{#aclModuleList}}        <li class="dd-item dd2-item aclModule-name" id="aclModuleTree_{{id}}" href="javascript:void(0)" data-id="{{id}}">            [{{id}}-{{name}}]            <ol class="dd-list">                {{#aclList}}                    <span data-id="{{id}}">{{id}}-{{name}}</span> &nbsp;                {{/aclList}}            </ol>        </li>    {{/aclModuleList}}</ol></script><script type="text/javascript">    var aclTreeTemplate = $('#aclTreeTemplate').html();    Mustache.parse(aclTreeTemplate);    function renderAclTree(result, idElement) {        var rendered = Mustache.render(aclTreeTemplate, {aclModuleList: result.data});        $('#' + idElement).html(rendered);        recursiveRenderAclModule(result.data);    }    // 递归渲染权限模块列表    function recursiveRenderAclModule(aclModuleList) {        if (aclModuleList && aclModuleList.length > 0) {            $(aclModuleList).each(function (i, aclModule) {                if (aclModule.aclModuleList && aclModule.aclModuleList.length > 0) {                    var rendered = Mustache.render(aclTreeTemplate, {                        aclModuleList: aclModule.aclModuleList                    });                    $('#aclModuleTree_' + aclModule.id).append(rendered);                    recursiveRenderAclModule(aclModule.aclModuleList);                }            });        }    }</script>